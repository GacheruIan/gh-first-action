name: first_workflow
on:
  workflow_dispatch:  

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print hello
        run: echo "hello."

      - name: Print goodbye
        run: echo "good-bye."
